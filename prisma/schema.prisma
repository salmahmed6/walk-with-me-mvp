generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(uuid())
  email        String   @unique
  name         String?
  avatarUrl    String?
  provider     String?
  password     String?
  createdAt    DateTime @default(now())
}

enum WalkStatus {
  CREATED
  WAITING
  ACTIVE
  FINISHED
}

model Walk {
  id              String     @id @default(uuid())
  ownerId         String
  participantId   String?
  durationMinutes Int
  status          WalkStatus @default(CREATED)
  createdAt       DateTime   @default(now())
  startedAt       DateTime?
  endedAt         DateTime?
}

model WalkMessage {
  id        String   @id @default(uuid())
  walkId    String
  senderId  String
  content   String
  createdAt DateTime @default(now())

  Walk Walk @relation(fields: [walkId], references: [id], onDelete: Cascade)
}
